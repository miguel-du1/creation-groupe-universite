RewriteEngine On

RewriteBase /Site/

RewriteRule ^(controleur|modele|config|algorithmes)/ - [F,L]

RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
